<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
       //let number=parseInt(prompt("enter the number:"))
       //if(number%2==0 && number%3==0){
        //docuemnt.write("number is divisible by 2 and 3")
       //}else{
       // document.write("number is not divisible by nor 2 or 3")
       //}

       //let a=parseInt(prompt("Enter a value : "))
       //let b=parseInt(prompt("Enter b value : "))
       //let c=parseInt(prompt("Enter c value : "))
       //if(a>b && a>c){
        //document.write("a is greater than b and c")
       //}else if (b>a && b>c){
        //document.write("b is greater than a and c")
       //}else{
       // document.write("c is greater than a and b")
       //}

       //let a=parseInt(prompt("Enter a value : "))
       //let b=parseInt(prompt("Enter b value : "))
       //let c=parseInt(prompt("Enter c value : "))
       //if(a>b && a>c){
        //document.write(`${a} is greater than b and c`)
       //}else if (b>a && b>c){
        //document.write(`${b} is greater than a and c`)
       //}else{
        //document.write(`${c} is greater than a and b`)
       //}

       let a=parseInt(prompt("No of total days : "))
       let b=parseInt(prompt("No of days absent : "))
       let c=a-b
       let d=(c/a)*100
       if(d>75){
        document.write("eligible to write exam " +d+ "%")
       }else{
        document.write("not eligible " +d+ "%")
       }
